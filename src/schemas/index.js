import signinSchema from './signinSchema';
import signupSchema from './signupSchema';
import urlsSchema from './urlsSchema';

const schemas = {
    signinSchema,
    signupSchema,
    urlsSchema,
};

export default schemas;
